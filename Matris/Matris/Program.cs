using System;

namespace Matris
{
    class Program
    {
        static void Main(string[] args)
        {
            /*Console.Write("Informe o valor da matriz de ordem: ");
            int n = int.Parse(Console.ReadLine());

            int[,] mat = new int[n, n];

            for (int i = 0; i < n; i++)
            {
                string[] valor = Console.ReadLine().Split(' ');
                for (int j = 0; j < n; j++)
                {
                    mat[i, j] = int.Parse(valor[j]);
                }
            }

            for (int i = 0; i < n; i++)
            {
                for (int j = 0; j < n; j++)
                {
                    Console.Write(mat[i,j]);
                }
                Console.WriteLine();
            }

            for (int i = 0; i < n; i++)
            {
                Console.Write(mat[i, i] + " ");
            }

                int conut = 0;
            for (int i = 0; i < n; i++)
            {
                for (int j = 0; j < n; j++)
                {
                    if (mat[i, j] < 0)
                    {
                        conut += mat[i, j];
                    }
                }
            }
            string nome = "john";
            bool s1 = string.IsNullOrEmpty(nome);
            bool s2 = string.IsNullOrWhiteSpace(nome);*/
            DateTime h1 = DateTime.Parse("11/07/2021");
            Console.WriteLine(h1);


        }
    }
}
